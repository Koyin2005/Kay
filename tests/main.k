type A = {
    b : int,
    c : int
}
type Three = 
| One
| Two
| Three
type Result[T,E] = | Ok(T) | Fail(E)

fun print_int(x : uint) = ()
fun what_the_helly() = do
    let a : Option[int] = return;
end
